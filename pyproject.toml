[project]
name = "dependency-parsing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch",
    "transformers",
    "datasets<3.0.0",
    "conllu",
]
